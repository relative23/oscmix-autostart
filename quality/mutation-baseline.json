{
  "recorded": "2026-08-28",
  "note": [
    "A ratio, not a count. Absolute survivor counts rise with every",
    "line added -- new code means new mutants, so a healthy change",
    "with good tests would trip an absolute ratchet. The score below",
    "is killed / (killed + survived): it stays comparable as the",
    "codebase grows and only moves when tests get better or worse.",
    "",
    "Not every survivor is a gap. About a quarter only change a log",
    "or error message, which nothing asserts on by design. Others are",
    "equivalent: `if not mismatched` decides when the read-back loop",
    "exits, not what it returns, so killing it would mean asserting on",
    "timing -- a worse test than the mutant is a risk.",
    "",
    "Mutants with no covering test are excluded from the score rather",
    "than counted as failures: the tests that drive a module through a",
    "real subprocess load the original source, so a mutation run says",
    "nothing about what they cover. See",
    "docs/decisions/0005-mutation-testing-scope.md.",
    "",
    "0.65 -> 0.67 (2026-08-19), and this is the largest rise so far,",
    "against the largest growth so far: 2726 -> 3745 mutants, over a",
    "0.688 -> 0.685 with 0.4.0's EQ family, and most of the fall is a",
    "property of the measurement rather than of the tests.",
    "",
    "`_eq_registers` alone carries 117 survivors -- the largest count of",
    "any function in the codebase, ahead of `build_arg_parser`. Three of",
    "them were applied by hand and run against the full suite:",
    "",
    "  prefix = None            import error, every test fails",
    "  \"XX/eqXX\" in the path    one test fails",
    "  tag \"I\" instead of \"i\"   one test fails",
    "",
    "All three are killed by tests that exist. mutmut selects the tests",
    "that *cover* a mutant from runtime coverage, and `_eq_registers`",
    "runs at module import inside the `UCX2 = Device(...)` literal, which",
    "is attributable to no test in particular. So the register table --",
    "the largest body of pure data in the project -- scores as though",
    "nothing tests it.",
    "",
    "This is the opposite of the 0.728 problem recorded above. There the",
    "score flattered the suite because uncovered mutants were excluded;",
    "here it understates it because covered mutants are not attributed.",
    "Both are reasons to read survivors rather than the number.",
    "",
    "What reading them actually found: no new gap in the behaviour-",
    "carrying code. The survivors in `_register_for` and",
    "`nested_families` are `split(\"/\", 1)` against `rsplit` or",
    "`split(\"/\", 2)`, which are equivalent while no path remainder has",
    "two slashes; the rest are message text. One real looseness turned",
    "up and was fixed: `settable_nested` answered for childless options,",
    "so `[gain:input:3]` was rejected only by a second guard elsewhere.",
    "",
    "0.68 -> 0.69 (2026-08-22), on the round trip for the dump's new",
    "sections. The score rose while the code grew, which is the unusual",
    "direction: a fixed-point test drives every branch of the renderer",
    "over the whole register table and compares the file byte for byte,",
    "so a mutant that survives a line-by-line assertion changes the file",
    "and dies. 4711 -> 4885 mutants, 0.685 -> 0.693.",
    "",
    "Measured locally on this tree at 0.693 (58 min, cache cleared), and",
    "on CI at 0.692 -- see confirmed_by_ci. The two agree to a thousandth,",
    "which they have not before: earlier CI runs sat well below the local",
    "score. Both are above the 0.68 floor this raise sets.",
    "",
    "0.69 -> 0.71 (2026-08-22), and this rise is a *denominator* change,",
    "not better tests. ADR 0015 exempts the register table from mutation:",
    "it is built at import time, which mutmut cannot attribute to a",
    "covering test, so it ran a subset that did not contain the test that",
    "would kill the mutant and reported a survivor the full suite kills.",
    "Verified by hand twice -- three `_eq_registers` mutants above, and",
    "`_seq` losing its `+ 1`, which fails",
    "test_an_option_the_channel_does_not_have_is_refused.",
    "",
    "159 survivors left the denominator; killed moved by 9. registers.py",
    "went from ~180 survivors to 14, and all 14 are in functions that",
    "*query* the table, which is where they belong. What now checks the",
    "table is tests/data/refresh-dump.json -- every path and every type",
    "tag against what the device reports.",
    "",
    "min_score follows floor_note's own rule rather than the rise: set at",
    "the measured score rounded down, so the policy neither fails nor",
    "nags. The reason it is safe to move now and was not before is that",
    "the erosion mechanism is gone -- auto level, low cut and crossfeed",
    "are the same shape as dynamics and will add their rows inside the",
    "exemption, so they cannot walk the score down 0.005 at a time.",
    "",
    "0.4.0 release run: 4866 mutants, score 0.711 against the 0.700 floor.",
    "The whole of `--diff`, Room EQ, low cut, crossfeed and the 802's",
    "capability map landed between this and the previous entry, and the",
    "score moved by a thousandth. That is ADR 0015 doing what it was",
    "written for: new register rows land inside the exemption, so what",
    "moves the number is assertions rather than data.",
    "",
    "0.71 -> 0.72 (2026-08-27), the 0.5.2 release run, and this rise is",
    "deletions as much as tests: reading the survivors removed a dead",
    "branch (17 unkillable mutants left the denominator) and added",
    "assertions where the reading found real gaps. The policy printed",
    "'raise min_score' itself, which is what this change does.",
    "",
    "0.72 held, not raised (2026-08-28): the 0.6.0 run measured 0.713.",
    "The fall is the price of the opened surface, and the floor at 0.71",
    "absorbed it as designed. Reading survivors for the next real gap is",
    "the way back up, not lowering min_score."
  ],
  "measured": {
    "total": 5017,
    "killed": 3511,
    "survived": 1414,
    "not_covered": 82,
    "score": 0.713,
    "run_minutes": 90,
    "cache": "cleared before the run, as CI does",
    "scope": "register table exempt (ADR 0015)",
    "note": [
      "The 0.6.0 release measurement, and the score fell 0.720 -> 0.713",
      "while the surface grew: opening Room EQ, output phase and the",
      "line gains added covered branches whose mutants are messages and",
      "equivalents, and the flipped refusal tests no longer kill what",
      "they used to. 0.003 above the floor. min_score stays 0.72 -- a",
      "ratchet is not walked back on the release that spends headroom;",
      "the next rise re-earns it."
    ]
  },
  "previous": {
    "recorded": "2026-08-27",
    "total": 5017,
    "killed": 3549,
    "survived": 1377,
    "not_covered": 82,
    "score": 0.72,
    "why_it_moved": [
      "The 0.5.2 release measurement; min_score 0.72 is set from it."
    ]
  },
  "confirmed_by_ci": {
    "run": 32537410701,
    "commit": "07ee560",
    "killed": 3316,
    "survived": 1476,
    "not_covered": 82,
    "score": 0.692,
    "note": [
      "The measurement this min_score is set from. Ten green jobs; the",
      "policy printed 'score rose to 0.692; raise min_score' itself,",
      "which is what this change does.",
      "",
      "Earlier CI runs measured below the local score (0.654 against",
      "0.685 on afed144). This one is above it, and the difference is",
      "the commit rather than the runner: CI ran the tree with the new",
      "round-trip tests, the local 0.685 did not."
    ]
  },
  "min_score": 0.72,
  "tolerance": 0.01,
  "floor_note": [
    "The gate is min_score - tolerance = 0.71. That is the number that",
    "matters: the score may not fall below it. min_score is set at the",
    "measured score rounded down to the hundredth, so the policy neither",
    "fails nor nags on the run it was recorded from -- a baseline that",
    "prints 'raise me' on every CI run teaches people to ignore it.",
    "",
    "0.010 of headroom on the measured 0.720, the same order as every",
    "raise before it. What eats headroom stayed the same: assertions",
    "getting better or worse, because register rows land inside the",
    "ADR 0015 exemption."
  ]
}
